#include "utils.h"

/*const Position* moyenne(Position *p1, Position *p2)
{
    return new Position((p1->getX() + p2->getX()) / 2,
			(p1->getY() + p2->getY()) / 2,
			(p1->getZ() + p2->getZ()) / 2,
			(p1->getDx() + p2->getDx()) / 2,
			(p1->getDy() + p2->getDy()) / 2,
			(p1->getDz() + p2->getDz()) / 2);
}*/
